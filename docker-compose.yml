services:
  smart_savings_bot:
    build: .
    container_name: smart_savings_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    working_dir: /app
    command: python -m app.main
    environment:
      - TZ=UTC
